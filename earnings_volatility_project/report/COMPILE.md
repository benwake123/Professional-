# Compile the paper

Run from the `report` directory:

```bash
pdflatex research_paper.tex
pdflatex research_paper.tex
```

The paper uses the generated tables in `generated/` and figures in `figures/`. A manual bibliography is embedded in the TeX file, so BibTeX is not required.
