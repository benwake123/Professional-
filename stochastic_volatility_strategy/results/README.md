# Expected Outputs

`daily_portfolio.csv`, `trades.csv`, `trade_decisions.csv`, `variance_forecasts.csv`, `model_calibrations.csv`, `pnl_attribution.csv`, `regime_attribution.csv`, `performance_summary.csv`, and `run_metadata.json`.
