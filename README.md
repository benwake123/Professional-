# Professional-

Quantitative ML alpha pipeline: daily equity features, walk-forward training, long/short backtest.

See `main.py` and `config/config.yaml`. Run locally after `pip install -r requirements.txt`.
